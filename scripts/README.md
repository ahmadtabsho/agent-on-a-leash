# Scripts

Developer helpers. The offline replay harness (step 5) lands here: it rebuilds
live-shaped events from the CSV pack and runs all 45 attempts through the engine
without touching the network.
